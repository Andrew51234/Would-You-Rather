import React, { Component } from 'react'
import { connect } from 'react-redux'
import { formatQuestion, formatDate } from '../Utils/api'
import { Link, withRouter } from 'react-router-dom'

class PollCard extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
